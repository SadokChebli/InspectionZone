apiVersion: v1
kind: Service
metadata:
  name: router
  namespace: inspection-zone
spec:
  selector:
    app: router
  ports:
    - name: http
      port: 8000
      targetPort: 8000
  type: ClusterIP
