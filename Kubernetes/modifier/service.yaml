apiVersion: v1
kind: Service
metadata:
  name: modifier
  namespace: inspection-zone
spec:
  selector:
    app: modifier
  ports:
    - name: http
      port: 8000
      targetPort: 8000
  type: ClusterIP
